version: 1.0
runtime: nodejs22
build:
  commands:
    build:
      - npm install --legacy-peer-deps
      - npm run build
run:
  command: npm start
  network:
    port: 3000
  env:
    - name: NODE_ENV
      value: production
