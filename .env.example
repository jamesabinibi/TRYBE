# Database
DATABASE_URL="inventory.db"

# Supabase Configuration
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Auth Secret
JWT_SECRET="super-secret-key-change-this"

# App URL (injected by platform, but for local dev)
APP_URL="http://localhost:3000"

# SMTP Configuration (Production)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
SMTP_FROM="StockFlow Pro <noreply@stockflow.pro>"
